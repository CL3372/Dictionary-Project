{
  "installCommand": "cd folder && npm install",
  "buildCommand": "cd folder && npm run build",
  "outputDirectory": "folder/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
